# Comprehensive Coverage Report — retired

This file is a stub. The report it held was generated on **2026-05-15** against branch
`claude/improve-test-coverage-AUK7z`, when the backend suite was 1,903 tests at 51%
coverage. Every module figure and gap in it predates the coverage work that followed and
is wrong today.

**Go to [`coverage-dashboard.md`](coverage-dashboard.md)** for current gates and counts,
and regenerate a per-line report with `--cov-report=html` (the command is in that file)
rather than reviving a checked-in one — a generated report in `docs/` goes stale silently
and cannot be diffed.
